/** @type {import('houdini').ConfigFile} */
const config = {
    watchSchema: {
        url: 'https://api.bapharia.com',
    },
    plugins: {
        "houdini-svelte": {}
    }
}

export default config