scalar Map

type Query {
	items: [Item!]!
	item(id: Int!): Item
}

type Item {
	id: Int!
	bapharia: BaphariaData
}

type BaphariaData {
	name: Text!
	desc: Text
	source: Text
	effect: Text
	thumb: String
	category: Text
}

type Text {
	ja_JP: String!
	en_US: String!
}