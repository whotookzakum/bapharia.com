<script>
    import MetaTags from "$lib/components/MetaTags.svelte";
    import LeafletMap from "$lib/components/leaflet/LeafletMap.svelte";
</script>

<MetaTags title={`World Map — Bapharia`} description={`Interactive map for BLUE PROTOCOL. Find enemies, locations, quests, treasure chests, gathering spots, and more!`} />
<LeafletMap/>
